# 古钱币识别系统（清币慧眼通）

基于深度学习的古钱币自动识别与分类系统，支持单币识别和批量识别功能。

### 本项目介绍的是"清币慧眼通——基于深度学习的古钱币识别系统"。

## 功能特点

1. **单币识别**：上传古钱币的正反面图片，系统自动识别币种信息
   - 如果正反面为同一种币种，只显示一种识别结果，避免重复显示
   - 识别结果包括：币种名称、朝代、材质、历史介绍、置信度

2. **批量识别**：提供高效的批量处理功能，满足大量古钱币的识别需求：
   - 单次最多支持上传 50 张图片,超过部分按50分批次，如181张分为四批
   - 批量上传超 50 个	识别前 50 个 + 弹窗 + 结果页提示超出数量，可选增加 “分批次识别” 按钮
   - 自动分批次，按币种分类展示和保存结果

3. **系统架构**：
   - 前端：HTML+CSS+JavaScript 实现用户交互界面（上传、展示结果）。
   - 后端：Flask 框架处理请求（图片上传、调用模型推理、结果存储）。
   - 算法核心：基于 ResNet18 的深度学习模型，完成 40 类（20 种钱币正反面）分类。

4. **数据流程**:
   用户上传图片→后端预处理→模型推理→结果匹配 / 分类→返回前端展示 + 保存到 results 文件夹。

## 识别币种列表（数据集共4135张图片）
数据规模：4135 张图片，40 个类别（20 种钱币的正反面），按 8:2 划分为训练集（3308 张）和验证集（827 张)<br>

系统包含20种古钱币的详细信息：<br>
   - 币种名称
   - 所属朝代
   - 材质（铜/银）
   - 简短历史介绍

1. 大清铜币
2. 大清银币
3. 道光通宝
4. 光绪通宝
5. 光绪元宝
6. 光绪重宝
7. 嘉庆通宝
8. 康熙通宝
9. 利用通宝
10. 祺祥通宝
11. 乾隆宝藏
12. 乾隆通宝
13. 顺治通宝
14. 同治通宝
15. 同治重宝
16. 咸丰通宝
17. 咸丰重宝
18. 宣统通宝
19. 雍正通宝
20. 昭武通宝

## 系统演示要点
1. 正常单币识别流程
- 上传正反面图片
- 展示匹配结果和详细信息
2. 批量识别功能
- 上传多张图片
- 展示自动分组效果
3. 异常情况处理
- 不匹配的正反面
- 识别失败的图片

## 我们系统支持单张正反面识别，也支持正反面配对验证
## 为什么需要 “正反面配对识别”？
1. 收藏者 / 鉴定师判断一枚钱币的完整信息，必须结合正反面特征,符合专业鉴定流程
2. 交叉验证提升识别准确率,单张识别可能因图片模糊、特征不全出错，正反面配对可交叉验证：<br>
比如正面图片模糊，识别为 “康熙通宝（置信度 60%）”，但反面清晰，识别为 “康熙通宝（置信度 95%）”，系统可结合正反面提升最终置信度至 90%
3. 防止用户误传图片导致错误识别

## 技术实现
### 数据集与预处理
  - **数据规模**：4135 张图片，40 个类别（20 种钱币的正反面），按 8:2 划分为训练集（3308 张）和验证集（827 张）。
  - **数据增强**：训练集采用旋转（15°）、水平翻转、色彩调整、归一化等，提升模型泛化能力；验证集仅做 Resize 和标准化
  - **理数据不平衡问题**：采用加权随机采样（少样本类别权重高）和类别加权损失函数（进一步均衡），双重策略缓解不平衡影响。每轮训练后验证，早停避免过拟合，同时保留验证集最优模型

### 模型选择与训练
1. **模型选型**：核心用ResNet18（残差网络 18 层），基于 ImageNet 预训练权重微调；
选择原因：
- 适配钱币识别场景：ResNet 的残差结构解决了深层网络梯度消失问题，能有效提取钱币纹理（文字、纹路、轮廓）等细粒度特征，而钱币识别的核心是纹理 / 细节匹配；
- 效率优先：ResNet18 比 ResNet50/101 更轻量，训练 / 推理速度快（适合批量识别场景），且 40 类分类任务无需过深网络（避免过拟合）；
- 预训练优势：基于 ImageNet 预训练，能复用通用视觉特征，少量钱币样本即可快速收敛（相比自建 CNN，无需从零训练，节省数据和时间）；
- 对比排除：VGG 参数量大、易过拟合；自建 CNN 无残差结构，深层训练梯度消失，特征提取能力弱于 ResNet。
训练集准确率最高约98%，验证集准确率约93%
- 相对于简单网络的优势<br>
残差结构保证深层网络训练稳定性<br>
特征提取能力强于浅层CNN<br>
具备良好的泛化能力<br>
这种设计使 ResNet18 成为图像分类任务的理想选择，特别适合古钱币这类需要精细纹理识别的应用场景。<br>
2. **训练细节**：
 - 输入尺寸 224×224，优化器 Adam（学习率 0.001），学习率调度器（10 轮后乘 0.1），早停机制（耐心值 3）。
 - 训练结果：训练集准确率～98%，验证集准确率～93%，模型保存为best_coin_model.pth。
3. **ResNet18优势**:
- **依托预训练权重的特征提取能力**：ResNet18 在model_2.py中使用了 ImageNet 预训练权重（models.ResNet18_Weights.DEFAULT），这些权重包含了从海量图像中学习到的通用视觉特征（如边缘、纹理、形状等）。对于钱币分类任务，这些预训练特征可以作为高质量的初始特征表示，减少模型对小样本数据集的依赖，尤其适合类别数较多（40 类）的场景。<br>
- 相比之下，model_3.py的定制 CNN 是从零开始训练的，需要从数据中学习所有特征，在数据量有限时容易过拟合或收敛缓慢
- **残差连接解决深层网络训练难题**：ResNet18 的核心优势是通过残差连接（Skip Connection） 缓解了深层网络的梯度消失问题，使得网络在达到 18 层深度时仍能高效训练。这保证了模型可以学习到更复杂的特征组合（如钱币的细节纹理、边缘轮廓等）。<br>
- 定制 CNN（model_3.py）虽然设计了 4 层卷积，但网络深度和复杂度远低于 ResNet18，对高阶特征的捕捉能力较弱，可能难以区分相似类别的钱币（如面额接近、图案相似的样本）。<br>

## 注意事项

- 上传的图片格式支持：PNG, JPG, JPEG, GIF, BMP
- 单次批量上传最多50张图片
- 识别后的图片会自动按币种分类保存到`results/`文件夹
- 系统会自动提取币种名称，相同币种会合并显示

## 系统要求

- Python 3.8+
- PyTorch 2.0+
- Flask 3.0+
- 模型文件：`best_coin_model.pth`

## 安装步骤

1. 安装依赖：
```bash
pip install -r requirements.txt
```

2. 确保模型文件存在：
   - 模型文件应位于项目根目录：`best_coin_model.pth`
   - 数据集文件夹：`数据集/`（包含40个类别的训练数据）


## 项目结构

```
.
├── app.py                  # Flask Web应用主文件
├── predict.py              # 模型推理模块
├── coin_info.py            # 币种信息数据库
├── best_coin_model.pth     # 训练好的模型文件
├── templates/
│   └── index.html          # 前端界面
├── uploads/                # 上传文件临时存储
├── results/                # 识别结果分类存储
├── 数据集/                  # 训练数据集（40个类别）
├── requirements.txt         # Python依赖
└── README.md               # 项目说明
```
系统结构<br>
主应用入口：app.py<br>
模型推理模块：predict.py<br>
币种信息数据库：coin_info.py<br>
训练好的模型文件：best_coin_model.pth<br>
前端页面：templates/index.html<br>
临时存储：uploads/文件夹<br>
结果存储：results/文件夹<br>

model_2.py 负责把数据集训练成可用的模型<br>
predict.py 把模型封装成可调用的推理工具<br>
app.py 则通过 Flask 路由把推理功能暴露成 Web 接口，让用户能通过前端页面上传图片、获取识别结果。<br>
整个流程实现了从数据到模型，再到应用<br>

predict.py核心作用是将训练好的 ResNet18 模型封装为可直接调用的预测工具，<br>
实现单张 / 批量古钱币图片的类别识别，为前端 / 业务系统提供 “输入图片路径→输出币种 + 置信度” 的核心能力<br>
核心定位: 作为训练好的模型与实际业务场景的 “桥梁”：<br>
1. 训练阶段：模型学习 “钱币图片→币种” 的映射关系；<br>
2. 推理阶段：本模块加载训练好的模型权重，接收图片输入，输出识别结果（币种名称 + 置信度）；<br>
3. 最终落地：可被 Web 系统、桌面程序等调用，支撑 “上传钱币图片→识别币种” 的核心功能。<br>


## 技术栈

- **后端**：Flask(一款轻量级、灵活的 Python Web 微框架)
- **深度学习**：PyTorch + ResNet18
- **前端**：HTML + CSS + JavaScript
- **图像处理**：PIL/Pillow

## 开发说明

- 模型训练代码：`model.py`
- 图像预处理：`image_process.py`
- 模型推理：`predict.py`
- Web应用：`app.py`

